# .github/workflows/tempo_detection.yml
name: Tempo Detection

on:
  push:
    branches:
      - main

jobs:
  detect_tempo:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.8'

    - name: Install dependencies
      run: |
        sudo apt-get install -y libsndfile1  # Install libsndfile
        python -m pip install --upgrade pip
        pip install librosa
        pip install PySoundFile  # Install PySoundFile and its dependencies

    - name: Find Tempo/BPM
      run: |
        python scripts/tempo_detection.py  # Adjust the path to match your directory structure


